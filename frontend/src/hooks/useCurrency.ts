/**
 * Currency Hook
 * Re-exports the useCurrency hook from CurrencyContext
 * This maintains backward compatibility while using the context provider
 */

export { useCurrency } from '@/contexts/CurrencyContext';
