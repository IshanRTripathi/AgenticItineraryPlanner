package com.tripplanner.service;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

/**
 * Service for travel planning tools.
 * 
 * NOTE: This service is currently a placeholder for future tool implementations.
 * Most tool functionality is now implemented directly in ToolsController.
 */
@Service
public class ToolsService {
    
    private static final Logger logger = LoggerFactory.getLogger(ToolsService.class);
    
    // Future tool implementations will go here
    // Current tools (calculate-cost, generate-node-id, check-user-constraints) 
    // are implemented directly in ToolsController
}

